# docs

Aggregated `typedoc --json` API snapshots published by CI, one file per project/ref:

```
<project-name>/<git-ref>.json
```

Written by `wolfstar-project/.github/.github/workflows/reusable-documentation.yml`.
Not meant to be edited by hand.
